# srslte-docs
The docs of srsLTE require sphinx, and the sphinx-seqdiag and sphinx-blockdiag extensions.

On Ubuntu, you can do:
sudo apt install python3-sphinx python3-sphinxcontrib.seqdiag python3-sphinxcontrib.blockdiag

For newer version you might also have to do:
sudo apt-get install python3-pip
sudo pip3 install sphinx_rtd_theme

To make the documentation do 'make html' on the main ./srsDOC/srslte_user_manuals folder
